# 皖域择岗档案 · 数据字典

构建版本 v14.0 · 快照 2026-08-31 · 2024—2026 三周期 · 自动生成，勿手改。

## 先读：真实性与完整性边界

本字典描述正式 HTML 使用的三年统一 bundle。内部完整是指：已收集源包的岗位行、招录人数、字段结构、聚合结果和页面载荷可以复跑对账；它不等同于所有外部官方逐岗材料均已发布或取得。未发布、未取得或无法唯一匹配的值保持空值，并在审计与页面中显示状态，不转写成 0。

- 三年审计报告：`docs/三年数据真实性与完整性审计_v12.md`；机器结果：`tools/anhui_web/data/three_year_audit.json`。
- 正式入口：`deliverables/皖域择岗总览.html`；三个周期共用一个 HTML，通过周期切换读取对应 payload。
- 证据状态：`verified`、`source_bundle`、`unpublished_or_unavailable`、`ambiguous_join`；具体含义以审计报告为准。

## 三年岗位数据集

| 周期 | 岗位行 | 招录人数 | 省考 | 事业编 | 国考 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2024 | 10,017 | 15,331 | 4,243 / 7,235 | 5,215 / 6,911 | 559 / 1,185 |
| 2025 | 10,150 | 14,721 | 4,116 / 6,620 | 5,491 / 6,956 | 543 / 1,145 |
| 2026 | 8,511 | 12,006 | 3,784 / 5,791 | 4,176 / 5,065 | 551 / 1,150 |

合计：28,678 岗 / 42,058 人。考试列为“岗位 / 招录人数”。

### 行级身份、来源和展示字段

| 字段 | 类型 | 覆盖行数 | 说明 |
| --- | --- | ---: | --- |
| `job_id` | string | 28,678 | 周期级稳定 ID；由 cycle、考试类别、城市、职位代码、单位和源表职位身份生成的 SHA-256 前缀，不含招录人数，人数修订不会换 ID。 |
| `row_id` | string | 28,678 | v12.1 向后兼容的技术行 ID，当前与 `job_id` 对齐。 |
| `legacy_row_id` | string | 8,511 | 仅在旧载荷存在时保留，用于迁移追溯；不作为新的收藏/对比主键。 |
| `city` / `exam` / `code` / `unit` / `num` | string / int | 28,678 | 城市、考试类别、职位代码、单位、招录人数；三年审计的必核主字段。 |
| `source_note` | string | 28,678 | 行级来源提示；历史载荷缺少该字段时保持空值，不补写来源。 |
| `cycle` | string | 14,882 | 所属周期；由统一 bundle 注入并用于周期级隔离。 |
| `title_status` / `display_title` | enum / string | 28,678 / 28,678 | 原表单列职位名称则为 `published`；未单列时显式显示“源表未单列披露”，不把单位名冒充职位名。 |
| `job_status` | enum | 28,678 | `active` 或定向核除相关状态；原始核除行保留在档案层。 |

### 成绩观测字段（禁止跨量纲比较）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `score_observation` | object | 28,678 行均有显式状态：`comparable`、`unavailable`、`suspected_sentinel`、`incompatible_scale` 或 `not_applicable`。 |
| `score_observation.scale_id` | enum / null | 当前可比量纲：事业编合成分 `syb_300`、省考百分制 `province_100`；空值不得进入分数模拟。 |
| `score_observation.value` | number / null | 原始入围线值；缺失保持 null，0 仅作为疑似哨兵值，不解释为真实成绩。 |

### 竞争观测字段（报名人数与考试人数分开）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `competition_observations.registrations` | object | 报名/缴费阶段分母；数值 0 标记为 `suspected_sentinel`，不作为实测竞争人数。 |
| `competition_observations.examinees` | object | 有效笔试/达线人数分母；与报名人数不相加、不混合。 |
| `competition_metric_type` | enum / null | 逐行首选口径：`examinees`、`registrations`、国考 `interview_shortlisted` 或空值。 |
| `ratio_comparable` | boolean | 仅在聚合分母类型单一且覆盖完整时为 true；混合分母、缺失分母、零哨兵或覆盖不完整时为 false，页面显示“不可比”。 |

## 三年质量状态分布

### 成绩状态

| 周期 | 可比 | 未取得/缺失 | 疑似哨兵 | 量纲不兼容 | 不适用 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2024 | 5,681 | 961 | 0 | 2,816 | 559 |
| 2025 | 5,198 | 1,747 | 0 | 2,662 | 543 |
| 2026 | 4,657 | 1,125 | 778 | 1,400 | 551 |

### 竞争分母类型

| 周期 | 考试人数 | 报名人数 | 国考进面名单 | 未取得/不可选 |
| ---: | ---: | ---: | ---: | ---: |
| 2024 | 8,291 | 985 | 551 | 190 |
| 2025 | 8,415 | 15 | 537 | 1,183 |
| 2026 | 6,909 | 872 | 546 | 184 |

### 职位名称披露状态

| 周期 | 源表单列 | 未单列披露 |
| ---: | ---: | ---: |
| 2024 | 4,802 | 5,215 |
| 2025 | 4,659 | 5,491 |
| 2026 | 5,491 | 3,020 |

## 质量聚合字段

- `jobs.cities[].competition_metrics`：按城市分别保存 `examinees`、`registrations`、`interview_shortlisted` 的 base / recruits / coverage_rows / ratio；不把不同观察类型叠加为一个分母。
- `jobs.cities[].ratio_status`：`single_denominator`、`mixed_denominators`、`partial_coverage`、`unavailable`。只有 `single_denominator` 且 `ratio_comparable=true` 才允许排名、平替和推荐逻辑使用竞争比。
- 2024/2025 的历史岗位聚合如果没有完整逐行分母，保留岗位数据但不生成虚假的城市竞争比；2026 的岗位观测台展示上述质量状态。

## 成绩名单与安全关联

| 周期 | `by_key` 岗位键 | 无法唯一匹配 |
| ---: | ---: | ---: |
| 2024 | 4,087 | 0 |
| 2025 | 4,107 | 0 |
| 2026 | 6,521 | 116 |

成绩复合键为 `exam|city|code|recruits`。2026 成绩附件缺少城市且职位代码对应多个候选岗位的 116 条记录进入 `ambiguous_join`，不写入任一岗位；不能用代码单字段猜城市。

## 待遇数据集

- 身份 × 工龄：公务员 / 事业编 × 刚入职 / 1 年 / 3 年 / 5 年 / 10 年，共 160 个锚点。
- 口径：年度全包估算中位数（应发 + 津补贴 + 公积金 + 年终等折算），为 2026 快照，不是政策工资、个人收入承诺或录用承诺。

## 当前公开缺口

### 2024

- `unpublished_or_unavailable` / `medium`：省考合格/缴费官方无逐岗表（全空）。证据：`tools/anhui_web/data/cycles/2024/cycle.json`。
- `unpublished_or_unavailable` / `medium`：拟聘用市级批次约 30+ 批未回收。证据：`tools/anhui_web/data/cycles/2024/cycle.json`。
- `unpublished_or_unavailable` / `medium`：事业编下半年官方原始 xlsx 未逐单位回收。证据：`tools/anhui_web/data/cycles/2024/cycle.json`。

### 2025

- `unpublished_or_unavailable` / `medium`：省考合格/缴费官方无逐岗表（全空）。证据：`tools/anhui_web/data/cycles/2025/cycle.json`。
- `unpublished_or_unavailable` / `medium`：事业编逐岗报名人数官方未发布（仅考区汇总）。证据：`tools/anhui_web/data/cycles/2025/cycle.json`。
- `unpublished_or_unavailable` / `medium`：拟聘用官方附件仍有阜阳批次未回收（蚌埠/池州/铜陵/宣城已补采）。证据：`tools/anhui_web/data/cycles/2025/cycle.json`。

### 2026

- `unpublished_or_unavailable` / `high`：2026 事业编成绩收割边界。证据：`tools/anhui_web/data/manifest.json`。
- `ambiguous_join` / `high`：成绩附件存在 116 个无法唯一匹配项。证据：`tools/anhui_web/data/score_lists.json`。

## JS 纯函数与交互安全边界

- `score-metrics.js`：仅接受带 `score_observation` 且同一 `scale_id` 的可比成绩。
- `product-core.js` / `product-jobs*.js`：竞争比消费统一经过 `competition_metric_type` 和 `ratio_comparable` 门禁；不同分母类型不用于平替或排名。
- `cycle-runtime.js`：周期切换在同一 HTML 内重新挂载周期载荷，不强制 `location.reload()`；本地收藏、对比和备注按周期隔离。
- `encodeShare / decodeShare`：本地状态分享码；不上传数据，不依赖网络。

## 相关文件

- 审计：`docs/三年数据真实性与完整性审计_v12.md`
- 单文件反解析复核：`docs/三年单文件数据复核报告_v12.md`
- 构建入口：`build.ps1`、`tools/anhui_web/release.py`
- 质量实现：`tools/anhui_web/data_quality.py`、`tools/anhui_web/unified_cycle_bundle.py`
- 页面校验：`tools/anhui_web/verify_single_file_v12.py`

## v14.0 维护站模块字典

维护站正式入口为 `deliverables/maintainable/index.html`，模块路径完全由 `data/site-manifest.json` 解析。HTML 只保存界面壳和加载逻辑，不保存完整岗位行。

| 模块 | schema | 关键字段 | 更新方式 |
| --- | --- | --- | --- |
| `overview.json` | `wanyu-maintainable-overview/v1` | `cycle`、`allMajors.meta`、`auditSummary` | 摘要或周期审计变化时重建 |
| `jobs.json` | `wanyu-maintainable-jobs/v1` | `allMajors.meta`、`allMajors.rows` | 岗位源表或岗位字段变化时重建 |
| `catalog.json` | `wanyu-maintainable-catalog/v1` | `majors`、`major_counts`、`facets` | 从当前 `jobs.json` 重新派生 |
| `positions.json` | `wanyu-maintainable-position-index/v1` | `rows[].record_id`、检索字段、`source` | 从当前岗位行重建，不独立编辑 |
| `scores.json` | `wanyu-maintainable-scores/v1` | `bs`、`ms`、`by_key`、`keyed.unresolved`、`summary` | 成绩源包收割/修正时替换 |
| `changes.json` | `wanyu-maintainable-changes/v1` | `base_cycle`、`target_cycle`、`changes`、`comparable`、`matching_policy` | 相邻周期岗位变化时重建 |
| `audit.json` | `wanyu-maintainable-audit/v1` | `audit`、`cycleInfo`、缺口、来源 | 每次源材料或审计状态变化时重建 |
| `three-year.json` | `wanyu-maintainable-audit/v1` | 三年周期数组与合计 | 三年 bundle 全部重建 |
| `review-queue.json` | `wanyu-maintainable-review-queue/v1` | `items`、`summary`、出现次数 | 审计缺口和复核事件变化时重建 |

### v14.0 状态与空值

`verified` 是已对账的源事实；`source_bundle` 是已接入源包但需保留源包说明；`derived` 是从源事实计算的派生值；`unpublished_or_unavailable` 是未发布、未取得或只有汇总口径；`ambiguous_join` 是存在多个候选而不能唯一关联；`needs_review` 是已发现材料但尚未通过正式门禁。

`null`、空字符串和 `0` 不等价：`null`/空字符串表示没有安全的事实值，`0` 只在源明确为零时允许使用。前端展示层不得使用 `value || 0` 抹平状态。`scores.json.summary.unresolved` 是明确的无法唯一匹配数量；缺少该字段时应显示未提供，而不是自动写零。

### 记录级证据对象

岗位索引的 `source` 对象至少包含 `status`、`source_ref`、`source_locator`、`observed_at`、`method` 和 `note`。详情抽屉展示这些字段，用户可以从展示结论回到岗位原文模块与审计模块。

### 跨年可比性

岗位匹配优先 `exact_code`，其次 `composite_exact`；语义候选只进入 `needs_review`。`changes.json.comparable` 只描述匹配层面的可比性；人数趋势还需要考试类别、统计分母和证据层一致。报名人数、考试人数和国考进面名单不能合并为一个竞争分母。

### v14.0 三年汇总锚点

2024：10,017 岗 / 15,331 人 / 成绩待复核 0；2025：10,150 岗 / 14,721 人 / 0；2026：8,511 岗 / 12,006 人 / 116。三年合计 28,678 岗 / 42,058 人，公开审计边界 8 项。以上是当前交接包内部的可复现对账结论，不代表所有外部官方逐岗资料均已取得。
