# 00 · 计划任务：全站数据核验与功能缺陷清零（v10 完美化计划）

> 面向对象：接手的 AI（或人）。执行地点：项目根 `D:\AI\CatPaw\2\皖域择岗档案_网页产品化升级版_20260830_v9.4\`
> 前置阅读：本包《交接文档_皖域择岗总包v9.9.5_20260830.md》§0 三分钟速览（10 分钟内可开工）。
> 总原则：**先固化基线 → 只补不盖 → 每次变更必重建+回归+文档同步 → 一切结论可复现**。

---

## 1. 总目标与验收标准（DoD）

| # | 验收项 | 完成判据 |
|---|---|---|
| D1 | 数据正确 | §3 的 7 组对账矩阵全部 PASS；页面数字与基线断言零漂移（有意的修复除外，须留审计件） |
| D2 | 功能无缺陷 | §4 交互矩阵每格打勾；浏览器 walkthrough 截图存 `tests/artifacts/v10-*.png`；控制台零 error |
| D3 | 回归绿 | `python -m unittest tests.test_anhui_web`（41+）与 `node tests/test_wanyu_core.cjs` 全绿 |
| D4 | 技术债清零 | §5 三项技术债处理完毕或有书面延期理由（写进 manifest.open_items） |
| D5 | 文档同步 | HANDOFF.md / docs/数据字典.md / data/manifest.json 与最终数据一致，版本号推进 |
| D6 | 可交接 | 全部新增脚本可复跑；审计件齐全；重新打包交接 zip |

**当前基线（2026-08-30 v9.9.5 终态，阶段 0 须复核）**：
全岗位库 **8,511 岗 / 12,006 人**（省考 3,784 + 事业编 4,176 + 国考 551）；scoreCoverage bm 8,379 / hg 8,379 / jf 3,784 / adv 7,681 / line 7,607 / top 7,133 / hire 795；事业编 perExam bm 4,046 / adv 3,355 / line 3,278；成绩库 **3,385 条 / Σadv 221,259**；职位表 4,122 行；周期页 2025=10,150/14,721、2024=10,017/15,331。

---

## 2. 阶段 0：环境与基线固化（半天）

- [ ] 0.1 构建三页 + 两周期页：`powershell build.ps1`；`python tools/anhui_web/build_pages.py --cycle 2025 --output-dir deliverables`；同 2024。
- [ ] 0.2 双测试套件回归：`python -m unittest tests.test_anhui_web`；`node tests/test_wanyu_core.cjs`。记录通过数。
- [ ] 0.3 **基线固化**：写一次性脚本 `tools/anhui_web/snapshot_baseline.py`（用后可留作回归件），从三页 HTML 提取 examCounts/scoreCoverage/perExam/城市汇总/cycle KPI → 写 `tests/expected_baseline.json`。此后每次重建后 diff，任何漂移必须解释。
- [ ] 0.4 成绩库断言：`total==3385`、`Σadv==221259`、审计链 `source` 含 15 条、抽查 202606001(adv=3/top=239.6/line=227.8) 与 3010006(line=202.5)。
- [ ] 0.5 浏览器冒烟（paw）：navigate 三个主页面 + 两个周期页，evaluate 检查 `8511`、`12006`、`7681`、`7607`、`4176` 出现于渲染文本，cycle-switch 2024/2025/2026 三链接存在。截图存 artifacts。

## 3. 阶段 1：数据正确性全量核验（2~3 天，脚本化对账）

方法论：**源文件 → 生成 JSON → 页面内嵌** 三层对账；凡计数差都要定位到行级原因。复用 `source_data/_research/verify_v1_*.py / verify_v2_*.py` 的骨架改造成 `tools/anhui_web/verify_full_v10.py`，输出 `docs/核验报告_v10.md`。

- [ ] 3.1 **省考**：`source_data/anhui2026/shengkao_master.csv`（3,784 岗）↔ `all_majors_2026.json` ↔ 页面。核对：岗位数、招录人数合计、职位码唯一、bm/hg/jf 逐岗、城市分组。判据：三层完全一致。
- [ ] 3.2 **国考**：`source_data/anhui2026/raw/2026guokao_anhui.xlsx`（551 行）↔ `guokao2026.json` ↔ 页面。⚠️ 代码复用必须用复合键（部门代码+职位代码+用人司局）；确认 jinmian/bmrank join 后 adv 无串绑。
- [ ] 3.3 **事业编职位表**：`huatu_syb_2026.json`（4,122 行）↔ 页面 4,176（+档案 135+滁州官方表行，去重后）。核对：**2602 名单制**（东至 30 码仅池州正本存在，审计件 `_dedup_2602_dongzhi.json`）；109 组历史去重无回归（`_dedup_fuzzy_pairs.json`）；recruits 合计=12,006 的构成。
- [ ] 3.4 **事业编成绩库**：raw_syb_fugao/raw_syb_county/raw_syb_shengzhi 附件 ↔ `syb2026_daxian_all.json`（3,385/221,259）。抽 10 个码回原始附件重算；确认 129 库外码仍不参与 join；`line==0→null` 语义一致。
- [ ] 3.5 **省考成绩链**：`ahsk2026_scores.json`/`official`/`daxian_full`/`hire` 四文件与页面 adv/line/top 覆盖率对照；4 个无完整名单职位保持空值（非 0）。
- [ ] 3.6 **资格与定向**：`position_eligibility.json`（544 条）/`job_eligibility_exclusions.json`（44 条）与页面画像过滤联动正确；核除岗（四项目 26/退役士兵 15/随军 2）不在主 KPI；0801048 待复核标记保留；3 条国考备注状态登记。
- [ ] 3.7 **周期包**：2025/2024 各 9 文件 ↔ 周期页 KPI（10,150/14,721；10,017/15,331）；批次键 join 无串批；2024 的 133 个"镜像源合成"定向岗备注在页面可见。
- [ ] 3.8 **跨字段一致性**：examCounts 三项和=total；cities 各市和=总数；perExam 的 bm/adv/line ≤ total；`recruits` 与 Σnum 对账；所有 `%` 覆盖率分母口径在页面脚注可解释。

## 4. 阶段 2：功能与按钮无缺陷验收（2~3 天，paw 浏览器逐格打勾）

对每个 cell：操作 → 断言结果 → 截图。统一用 `paw browser-action`（本机可用，免装依赖；如装 playwright 亦可，脚本放 `tests/browser_v10.*`）。

- [ ] 4.1 **导航与视图**（file:// 直开 `deliverables/皖域择岗总览.html`）：全省总览 / 机会地图 / 岗位地图 / 全岗位库(#jobs_all) / 分数模拟 / 机会洞察 / 待遇地图 / 我的短名单 / 报考手册 / 原始档案 十视图各打开一次：标题渲染、主 KPI 有数、无空白面板。
- [ ] 4.2 **周期切换**：导航 2024/2025/2026 三向互切（含 `../2024/皖域择岗总览.html` 相对路径在 file:// 下可达）；周期页六视图（周期总览/岗位地图/全岗位库/岗位检索/岗位对比/短名单）可用，KPI 与 cycle.json 一致。
- [ ] 4.3 **顶栏功能键**：⌘ 命令面板（Ctrl+K 打开/跳转/岗位直达/Esc 关闭）、⚙ 条件（身份勾选→全站过滤联动：检索/对比/收藏/分数模拟数量变化；严格模式开关；被拦截行灰显+原因徽章；勾选退役士兵恢复被核除岗）、？名词/数据来源浮层、Aa 字号、◐ 主题切换（亮/暗持久化）、⌁ 打印视图。
- [ ] 4.4 **全岗位库内部**：考试类别 tab（省考/事业编/国考/全部）、城市筛选、资格 chips（13 类）、批量代码反向过滤（粘贴 20 个码）、排序、分页（第 1/172 页）、j/k/s/c 键盘流、行点击 → 详情抽屉（⚠️ 重点：跨市同码岗位详情绑定正确——国考 2 组真复用码、allMajors 219 组同码关联须用复合键命中，见 §5.1）、收藏星标、加入对比、城市短名单。
- [ ] 4.5 **对比/排名/检索**：对比视图两岗并排字段齐全；观测台/排名页竞争比带数据源标记（✓考试人数/≈报名回退）；检索页与全岗位库筛选状态互相同步且持久化（刷新后保留）。
- [ ] 4.6 **分数模拟**：事业编 300 分制 / 省考合成 100 分制分开输入 → 稳/达线/贴线/最近未达四档清单 + 入围线直方图渲染；限定城市后清单变化。
- [ ] 4.7 **待遇侧**：待遇地图/排名页 16 市 × 5 阶段数据完整；"起点与增速"散点渲染（1.6× 参考线、点色编码）；免责声明可见。
- [ ] 4.8 **状态管理**：本地状态导出 JSON → 清空 → 导入还原（对比/收藏/短名单/画像/筛选）；URL 分享码 decode 正确；`?metric=jobs#jobs_all` 直达。
- [ ] 4.9 **鲁棒性**：390×844 窄屏无页面级横向溢出（表格出滚动提示）；file:// 无控制台 error（evaluate 收集 window errors）；断网状态打开全部资源可加载（离线产品红线）。
- [ ] 4.10 缺陷登记：任何一格失败 → 记入 §9 进度表 → 修复 → 重跑该格 → 截图覆盖。

## 5. 阶段 3：已知技术债修复（1~2 天）

- [ ] 5.1 **详情绑定复合键（P2，核验报告 4.2 项）**：`templates/product-all.js` 详情查找、`build_score_lists.py` bs/ms 键、`build_pages.py` 档案 join——把 code-only / city+code 升级为 (exam, city, code[, num])；修完用 219 组同码样本回归验证无错绑。
- [ ] 5.2 **周期页成绩名单**：`build_score_lists.py` 增 `--cycle` 支持，为 2025/2024 生成 `cycles/{y}/score_lists.json` 并接入周期页。
- [ ] 5.3 **open_items**：0801048 官方复核（查 2026 省考职位表省直分表）；3 条国考备注向华图/官方源核对；结论写回 manifest（closed 或延期+理由）。

## 6. 阶段 4：时间触发任务（9 月中旬）

- [ ] 6.1 **东至 30 岗成绩收割**（唯一剩余数据缺口）：监控 `www.dongzhi.gov.cn`（新闻中心→公示公告）与池州市人事考试网，出现《2026年东至县事业单位公开招聘工作人员笔试成绩公告》后：
  `python tools/anhui_web/harvest_gov_page.py "<公告页URL>" dongzhi`
  白名单 30 码已在库（池州正本），工具自动只补不盖+审计链+备份。完成后：重建 → 基线 diff（line 预计 +≤30）→ 测试断言更新 → 文档同步。
- [ ] 6.2 如平台支持自动化，可建每周一次的"东至公告巡查"计划任务（只查不发版，发现即执行 6.1）。

## 7. 阶段 5：终验与再打包（半天）

- [ ] 7.1 零差异复核：重建后与 `tests/expected_baseline.json` diff（允许的漂移必须各有一张审计件）。
- [ ] 7.2 双套件全绿 + §4 矩阵全勾 + 截图归档。
- [ ] 7.3 HANDOFF/数据字典/manifest 版本推进（v10.x），变更总账表更新。
- [ ] 7.4 重打交接 zip 放桌面（沿用本包结构，交接文档版本号更新）。

## 8. 红线（违反即返工）

1. 成绩库合并**只补不盖**；白名单限职位表宇宙。
2. **2602 修正按 `_dedup_2602_dongzhi.json` 名单制**，严禁按前缀全局替换。
3. 不可改用"单位名互含"去重（实测误删 73 个同单位异码岗）。
4. 国考 join 用复合键（部门代码+职位代码）；事业编成绩 join 带（批次,代码）。
5. 华图附件是 2KB HTML 壳（u3.huatu.com 直链绕过）；县政府网 HTML 页 521 反爬、静态附件直链不拦。
6. `.backup.json` 是滚动快照，权威留档只有 `.pre0830.json` / `.pre_dongzhi.json` / 审计件。
7. 覆盖率分母口径：职位表行口径（含批次重复行）与页面口径（4,176）不同，报告时必须注明。
8. 省直 564 岗已穷尽证实不可得——**不再爬取**。

## 9. 进度登记表（执行时逐行填）

| 日期 | 阶段/任务# | 结果(PASS/FIXED/延期) | 证据(文件/截图) | 备注 |
|---|---|---|---|---|
|  |  |  |  |  |

——计划完。按 §2 开工，全部勾完即达成"数据全对、功能无缺陷"的 v10 完美态。
