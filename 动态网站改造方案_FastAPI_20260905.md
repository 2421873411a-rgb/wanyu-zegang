# 皖域择岗 - 动态网站改造方案

## 一、改造目标
将当前纯静态网站改造为动态网站，增加：
1. **用户系统**：注册/登录、收藏/快照云端同步
2. **实时数据管理**：后台管理界面
3. **服务器端搜索**：高性能搜索、分页、筛选
4. **数据统计分析**

## 二、技术架构

### 技术选型
- **后端框架**：FastAPI（Python异步框架，自带API文档）
- **数据库**：PostgreSQL（支持JSON字段，适合结构化数据）
- **缓存**：Redis（高性能缓存，会话管理）
- **ORM**：SQLAlchemy 2.0（支持异步）
- **认证**：JWT + OAuth2（无状态认证）
- **部署**：Gunicorn + Nginx + systemd

### 架构图
```
前端(现有SPA) → FastAPI后端 → PostgreSQL + Redis
        ↓
   静态JSON文件(保留作为备份)
```

## 三、数据库设计

### 核心表结构
1. **users** - 用户表（UUID主键，邮箱登录）
2. **jobs** - 岗位主表（从JSON导入，支持全文搜索）
3. **saved_positions** - 用户收藏表
4. **filter_snapshots** - 筛选快照表
5. **compare_lists** - 对比列表表
6. **cycles** - 周期元数据表
7. **score_index** - 成绩索引表
8. **salary_data** - 待遇数据表
9. **review_events** - 审计事件表

### 关键索引
- 全文搜索向量（tsvector）
- pg_trgm模糊匹配索引
- 城市/考试/专业筛选索引

## 四、API设计

### 认证API
- POST `/api/v1/auth/register` - 注册
- POST `/api/v1/auth/login` - 登录
- GET `/api/v1/auth/me` - 用户信息

### 岗位API
- GET `/api/v1/jobs/search` - 全文搜索+分页
- GET `/api/v1/jobs/{record_id}` - 岗位详情
- GET `/api/v1/jobs/ranking` - 城市排行

### 用户工作台API
- CRUD `/api/v1/user/positions` - 收藏管理
- CRUD `/api/v1/user/snapshots` - 快照管理

### 管理后台API
- POST `/api/v1/admin/import/{cycle}` - 数据导入
- GET `/api/v1/admin/dashboard` - 统计仪表盘

## 五、前端改造

### 改造原则
**最小化改动**：保留现有SPA结构，仅修改数据加载层

### 改造层次
1. **DataStore改造**：新增APIDataStore，双数据源（API优先，JSON fallback）
2. **UserStore改造**：localStorage + API双写同步
3. **新增组件**：登录模态框、管理后台视图
4. **Service Worker适配**：API请求绕过缓存

## 六、部署方案

### 服务器环境
- 腾讯云轻量应用服务器（4核4GB）
- Ubuntu 24.04 LTS
- PostgreSQL 16 + Redis

### 部署目录
```
/opt/wanyu/
├── api/          # FastAPI应用
├── static/       # 原有静态站
└── backups/      # JSON备份
```

### Nginx配置
- `/api/` → 代理到Gunicorn（8000端口）
- `/maintainable/` → 静态文件服务
- SSL证书：Let's Encrypt自动续期

## 七、实施计划（28天）

| 阶段 | 内容 | 耗时 |
|------|------|------|
| 0 | 环境准备（PostgreSQL/Redis/FastAPI骨架） | 2天 |
| 1 | 数据导入管道（JSON→PostgreSQL） | 3天 |
| 2 | 核心API开发（搜索/认证/用户工作台） | 7天 |
| 3 | 前端改造（双数据源/登录UI） | 5天 |
| 4 | 管理后台开发 | 4天 |
| 5 | 部署与上线 | 4天 |
| 6 | 文档与收尾 | 3天 |

## 八、风险与对策

### 高风险
1. **数据迁移不一致** → 编写校验脚本，保留JSON备份，双线并行2周
2. **4GB内存不足** → 优化PostgreSQL配置，限制Gunicorn workers
3. **搜索性能不达标** → tsvector预计算+pg_trgm索引+Redis缓存

### 回滚方案
- API层回滚（5分钟）：停止Gunicorn，Nginx直接提供静态文件
- 全量回滚（30分钟）：恢复原静态站部署

## 九、预期效果
1. 用户收藏/快照云端同步，支持多设备访问
2. 服务器端搜索，响应时间<200ms
3. 后台管理界面，实时更新数据
4. 保留离线访问能力（fallback到静态JSON）
