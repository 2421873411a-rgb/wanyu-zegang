# 皖域择岗 · 交接报告（2026-09-11）

> 交接包：`皖域择岗交接包_v17.9.2+b2_含密钥_20260911`（本目录的打包快照）
> 上一交接包：v17.8.6+b2_含密钥_20260907

## 一、当前状态（一句话）

网站 **v17.9.2** / API **v17.10.2**（RC，未打正式 tag）；分支 `hardening/night-20260911` 基于 GitHub main@`9f87d772` 领先 **21 个提交**，全部本地门禁绿；业务事实零变化（canonical 聚合 SHA `fe2303a3…` 前后一致）；wan-api 保持 **EXPERIMENTAL**（DR 未闭环，口径不虚报）。

## 二、本周期做了什么（三波）

1. **同步**：老交接包（main@5e1b5e8，v17.8.6+b2）快进到 GitHub main@9f87d77（v17.10.1 后 5 提交），本地密钥/交付物全部保留。
2. **夜间工程加固**（10 提交）：审阅确认的 6 个 P1 全修——profile 输入全局 event、DataStore SHA 真校验（fail closed）、导入 commit 后才失效统计缓存、nginx/字段双层资源边界、bcrypt 线程池化、整数契约严格化；外加快照配额事务锁、compare 槽位重试、worker 单一真源+启动不变量、npm 供应链、`site-release-gate` CI（漂移门禁+干净重建+三浏览器矩阵）、readiness_check、DR 证据 schema、风险登记册。
3. **两轮独立审计 + 修复**：第一轮 19 条（0 P0/P1）→ 修 13 条；第二轮双代理 28 条（0 P0、3 P1）→ 修 P1 全部 3 条 + 7 条 P2/P3（详见 `docs/self-iterate/2026-09-11-night-hardening/06、07` 两份处置台账，含刻意延后清单与理由）。

## 三、门禁证据（打包前实测）

| 门禁 | 结果 |
| --- | --- |
| 站点 Python 套件 | 237 passed / 6 skipped |
| 站点 verifier | 302 passed / 0 failed |
| 生成漂移门禁 parity | PASS（12 资产 + sw + 54 gz） |
| Node 契约 | 26 + 14 场景 PASS |
| Chromium 全流程烟测 | PASS（console 零报错，三 viewport） |
| API 套件（ENV=test） | 129+ passed（含 PG 专属并发用例，SQLite 下 skip） |
| 业务不变量 | IDENTICAL（10,017 / 10,150 / 8,511→8,401→11,883 / review 7+1 零变化） |
| 文档真源门禁 | PASS（README/current-status 与 v17.9.2 一致） |

## 四、接手必读（按优先级）

1. **推送**：本机 git 凭证未配置，分支尚未上 GitHub。`gh auth login` 后执行
   `git push -u origin hardening/night-20260911`，开 PR 合入 main。
2. **CI 首跑**：`site-release-gate` 从未在 GitHub 上运行过。首跑重点看三浏览器矩阵（Firefox/WebKit 本地未测）与干净重建比对（supplement 链在 CI 归一）。合入前建议把 `site-release-gate` 加进 main 必需检查。
3. **合并后动作**：RC2 破坏性验证（篡改 JSON/旧 SW 缓存/Redis 停机）→ 打 tag `v17.9.2`（site）/ `v17.10.2`（api）→ 发布页贴 `05-RELEASE-EVIDENCE.md`。
4. **审计遗留**：16 条刻意延后项全部记录在 `07-AUDIT2-REMEDIATION.md`，其中 API-006（生产导入缺 canonical 指纹闭环）与 FE-004（三年趋势页口径）建议优先立项。
5. **DR 现状**：本机 dump+演练齐备，但 COS 异地副本与异机恢复演练无证据，`dr_ready=false` 是真实的（证据格式已建好：`wan-api/docs/ops/dr-evidence.schema.json`）。

## 五、关键路径

| 内容 | 位置 |
| --- | --- |
| 密钥（勿入 git，已 gitignore） | `_密钥_勿入git/`（README_密钥与部署.md + PEM） |
| 数据源原始件（不入 git） | `项目源码/source_data/` |
| 正式站点 | `网站/`（v17.9.2，入口 index.html） |
| 数据真源 | `项目源码/canonical/`（本轮零变更） |
| 部署 | `项目源码/wan-api/deploy.sh` + `docs/ops/RUNBOOK.md` |
| 本轮全部工程记录 | `docs/self-iterate/2026-09-11-night-hardening/`（00~07） |
| 风险登记册 | `docs/engineering/RISK_REGISTER.md` |

## 六、本地快速验证

```bash
cd 项目源码
python tools/anhui_web/check_site_parity.py            # 模板↔网站 字节级
python tools/anhui_web/verify_maintainable_site.py     # 302 项站点校验
node tests/test_datastore_contract.cjs                 # 完整性矩阵
cd wan-api && ENV=test python -m pytest tests -q       # API 全套
```

## 七、版本号说明

`release.json`（网站）与 `wan-api/release.json`（API）是两个独立真源，禁止互相复制版本号。站点资产版本 `?v=17.9.2`、SW 版本 `wanyu-shell-v52` 随 release.json 注入。
