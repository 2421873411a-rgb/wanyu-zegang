本目录是一次 /self-iterate 运行（批次 2，承接批次 1 p0-w1-stoploss 的 EXIT）。恢复步骤：① 读 `state.json`；② 用一句话向用户复述目标与点位状态；③ 按 `baton` 继续；④ 遵守铁律（证据在案、每轮一个焦点、verify/ 只加强不削弱、双条件门才 done=true）。

**现场**：站点 `E:\zcode\择岗\网站/`（批次1后为 v17.7.1/SW v41 未发布态）；计划 `E:\zcode\择岗\五路审计汇总_升级计划_v2_20260905.md`；提交仍被 Mimosa 门禁挂起（改动留工作区，晨会裁决）。红线同批次 1：不部署、不连生产、未知不补 0、pem 不入 git、FastAPI 不动。
