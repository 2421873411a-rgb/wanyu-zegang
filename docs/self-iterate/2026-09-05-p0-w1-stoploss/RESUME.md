本目录是一次 /self-iterate 运行。恢复步骤：① 读 `state.json`；② 用一句话向用户复述目标与点位状态；③ 按 `baton` 继续；④ 遵守铁律（证据在案、每轮一个焦点、仅双条件门达成才 done=true）。

**目标一句话**：按《五路审计汇总_升级计划_v2_20260905.md》W1 止损清单（P0-1~P0-9 + D1 启动），对皖域择岗站（v17.7.0，本机 `E:\zcode\择岗\网站/`）做多轮「实现→验证→修正」自迭代，直到 09:00。

**关键现场**：
- 站点副本：`E:\zcode\择岗\网站/`（现役 v17.7.0 / SW wanyu-shell-v40）；源码工具：`E:\zcode\择岗\项目源码/`
- 计划原文：`E:\zcode\择岗\五路审计汇总_升级计划_v2_20260905.md`
- 验收脚本在 `verify/`（受削弱检查保护，不得削弱）
- 红线：不连生产/不部署；不改岗位原文；未知不补 0；111.pem 不进 git；勿跑 release.py 全量构建（工具链未同步前会回退成果）
- 本轮凌晨会话由 relay（计划任务）与主会话双通道续跑；写 state.json 前必读盘对 rev（乐观锁）
