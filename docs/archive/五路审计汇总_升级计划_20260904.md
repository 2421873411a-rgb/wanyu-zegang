# 皖域择岗 · 五路审计汇总与升级计划
> 审计日期：2026-09-04 · 基线版本 v17.6.13（SW v39）· 五路并行：数据质量 / 前端性能 / UX·IA·无障碍 / 产品功能 / 部署安全运维
> 总纲：**这个站不缺数据，缺的是把审计级数据翻译成决策的最后一公里。所有升级必须继承站规——未公布就不显示，推导值就亮明推导，LLM 永远不碰数字。**

---

## 〇、五路核心发现速览

| 路线 | 最重要的发现 |
|---|---|
| 数据质量 | 数据纪律业内罕见（job_id 零重复、SHA-256 全链、空值不推断）；但有 778 条 0 分哨兵、627 条跨量表分数线、116 条成绩因六安/马鞍山职位代码撞码无法归属、2024 年 city 字段 83 个脏值、职位族跨年覆盖率仅 29% |
| 前端性能 | v17 增强层（搜索历史/分享/Toast）是**从未接线的幽灵功能**，每次首屏白下载 43KB；检索页白白加载 5.3MB positions.json；点"查看详情"=16.7MB 下载；三年对照=38.8MB；`cache:'no-cache'`+network-first 导致每次进站全量重下 5MB+ |
| UX/IA | 落地页首屏仍是数据工程语言（"证据层/口径/岗位行"）；考生最高频的"找岗位"排在导航第 6 位；全局筛选（年份+考试类别）在移动端可被滚动隐藏；浅色主题辅助文字对比度系统性低于 AA（最低 2.01:1） |
| 产品功能 | 七个结构化字段（层级/机构系统/考试科目/应届/性别/党员/法考标签）在库中睡大觉；changes.json 逐岗 diff 已建好但前端没做视图；专业检索是裸子串匹配——"我能报什么"这个第一决策环节靠运气 |
| 部署安全 | 新包无密钥泄漏（历史 pem 问题已清）但**旧密钥对是否已在腾讯云重置无法从包内验证（唯一高危待办）**；wan-api 一键部署脚本有真 bug（SECRET_KEY 不展开、nginx 语法错）；README 部署命令引用不存在的目录；无 Git/无备份/无监控；网站-lite 与网站逐字节相同（125MB 冗余） |

---

## 一、立即行动（本周内，按序执行）

### 安全（半天）
- [ ] **S1 确认旧密钥已重置**：腾讯云控制台重置历史 `服务器密钥_111.pem` 对应密钥对（旧包已扩散该 pem，未重置=活凭据外泄）。15 分钟
- [ ] S2 固化 `tools/check_secrets.sh`：pem/key/api-key/token 四条 grep，对外发包前必跑
- [ ] S5 修 search-history.js 引号转义缺口 + share-link 二维码改本地生成（隐私+离线）

### 性能速赢（2 天，全站流量质变的前置）
- [ ] A1 从 index.html 摘除 v17 幽灵层 4 script + 3 css（搜索历史/分享/Toast 从未生效，白载 43KB）
- [ ] A2 检索页停载 positions.json（renderSearch 第三参被忽略，纯浪费 5.3MB）
- [ ] A4 DataStore 校验 encode 复用 + 哈希不匹配不再重试 13MB
- [ ] A5 click 委托 try/catch + unhandledrejection 兜底（弱网"点了没反应"可诊断）
- [ ] A6 SW PRECACHE 补齐 10 个漏项 + icon 版本对齐

### 数据补漏（1-2 周）
- [ ] D1 六安/马鞍山 0901 段 116 条成绩归属核对（两市人社局职位表原文，约半天人工）
- [ ] D2 778 条 0 分哨兵回溯原始公告（芜湖229/安庆162/淮北123…），区分缺考/未发布
- [ ] D3 东至县下半年 30 岗成绩（9 月中旬公告发布即收割）
- [ ] D4 公告原文链接挂接（supplement 台账已有 43 个文件的 sha256，只差挂接）

---

## 二、30 天升级路线（两线并行）

### A线：体验升级（UX + 前端）
1. **落地页首屏改造**：hero 标语改考生行动语；加"输专业立刻看岗位"直达框+热门专业chip；"证据层/口径/岗位行"等首屏术语全部考生化；状态条"外置模块已加载·N行"→"2026年数据已就绪·共N个岗位"
2. **全局筛选显性化**：年份+考试类别拆成带标题的筛选条，补 aria-pressed，移动端换行不滚动；筛选≠全部时每页标题强制带后缀
3. **IA 重排**：导航改为「找岗位→岗位地图→待遇对比→城市排行→三年趋势→我的收藏→数据说明」；岗位检索提至首位；"周期总览"降级为首页数字条；四处"数据审计/数据说明"统一命名
4. **无障碍三件套**：移除 main 的 aria-live；SVG 去 role=img 改 group；地图标签去 tabindex（49个Tab停靠点减半）
5. **对比度系统性修复**：--text-muted 系提到 ≥4.5:1（#5b7391 方案），竞争三色换语义 token，空状态文字加深
6. **数据缓存内容寻址**：JSON 请求 URL 追加 `?sha=`（manifest 已有每模块哈希），带 sha 走 cache-first——回访流量从 5-14MB 降到 8KB。**全站最大流量优化**
7. **检索/筛选状态入 URL**：可分享筛选结果链接，刷新不丢条件

### B线：产品升级（决策链补缺，赶 10 月底 2027 国考报名周）
1. **T1 专业智能匹配引擎**（6-8天）：教育部目录树 + 别名词典 + 倒排索引；三级匹配（明确含专业名/按类可报/不限）绝不静默合并，每条显示匹配依据原文——解决"知识产权搜不到法学类岗"的致命缺口
2. **T2 资格画像+一票否决**（3天）：学历/学位/年龄/性别/党员/应届/法考/四项目画像存本地，全站岗位自动标"可报/限男性/限应届"，age 文本解析器 build-time 预计算；与 T1 交集=真正的"我能报什么"
3. **深水区筛选器**（2天）：把在库中沉睡的 7 个结构化字段（cc层级/xz机构系统/lb类别/km考试科目/tags应届性别党员法考/dir定向）做成筛选项
4. **T5 变更通报视图**（2天）：changes.json 逐岗 diff 已建好，纯前端渲染——"你收藏的岗从招3改招1了"是杀手级回访理由

### 运维（穿插）
- [ ] R1 Git 化项目源码 + COS 异地备份（当前无版本控制是最大单点）
- [ ] E1 CDN/brotli/HTTP2（3Mbps 带宽下首屏 30 秒→2-3 秒）
- [ ] R3 修 README 部署断链（verify 命令指向不存在目录）+ R4 存档生产 nginx 配置
- [ ] E2 下一版交接包删除网站-lite（与网站逐字节相同，125MB 冗余）
- [ ] E5 jobs.json 构建期预压缩 .gz/.br

---

## 三、季度路线（择机）

| 专项 | 结论 |
|---|---|
| 拆 ES 模块 | **做**。已是 module 加载，机械拆分 3-5 天，是后续一切的前置 |
| 按视图懒加载 | **做，收益全站第一**。构建期预聚合 agg.json（<100KB），三年对照从 38.8MB 归零；详情改 jobs_lite+positions 合成 |
| 检索索引化 | 做，排在拆分后；顺带恢复关键词搜索输入框（现役检索页无关键词框，属功能回退） |
| CSS 合并 | 做。v17-ui-upgrade 重定义了 69+64 个 token、78 个选择器与基础样式逐字重复——变量归一后合并 9 文件为 3 |
| 渲染局部更新 | 做。render() 拆骨架+结果区两级，可整体删除 IME/焦点三补丁 |
| 上岸难度指数 | 数据全具备（bm/num/line/job_history/cc），build-time 预计算 difficulty.json，四处复用 |
| 分数洞察 | 三年入围线折线+考分反推器（利用 archive/scores 2422 条进面分）；**scale_id 硬闸门防跨量表混算** |
| 变更通报+决策清单+ICS日历 | 纯前端可达的"最接近推送"方案 |
| LLM 问答 | **NL→Filter 规则引擎先行**（0成本零幻觉覆盖70%问法）；LLM 只做解释，数字由本地计算注入；BYOK 路线备用 |
| 微信分发 | 公众号立即做（成本≈0，H5 直用）；小程序缓一步（需备案+ICP+UI重写 15-25 人日，先验证留存再投入） |
| wan-api | 暂不启用。骨架 80% 但 3 个部署 bug + 无迁移无测试；启用前先修 python-jose CVE |

---

## 四、需要站长决策的事项

1. **S1 密钥重置是否已完成？**（包内文档显示是待办，无法验证）——这是唯一未闭环的高危项
2. **网站-lite 去留**：与网站逐字节相同，建议下一版删除或做出真差异（如去掉 scores）
3. **wan-api 启用时机**：建议保留为实验分支，多端同步需求出现后再修 3 bug 启用
4. **升级执行模式**：本汇总的"立即行动+30天路线"是否由我继续执行，还是并入下一轮交接

---

## 五、不能丢的东西（五路审计一致公认）

- 空值纪律："未发布保持空值，不推断为零"
- 来源可核对：SHA-256 全链 + 证据台账 + 匹配依据原文展示
- 诚实边界：省直不伪造、量表不混算、竞争未知就标未知
- 零运行时依赖、零第三方外呼的架构纪律（二维码外呼是唯一违例，待修）
