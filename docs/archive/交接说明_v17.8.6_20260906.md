> ⚠️ **ARCHIVED — 历史存档，不再作为当前状态入口。** 当前状态唯一入口：`README_先看这里.md` + `docs/current-status.md`（真源：release.json/site-manifest/review-queue）。

# 皖域择岗 · 交接说明（2026-09-06 · v17.8.6 正式发布态）

> 本文档取代 `交接说明_20260905.md` 成为最新交接入口（后者与 `交接包清单.txt` 保留作历史参考）。
> 结论先行：**v17.8.6 已正式发布并打 tag（tag v17.8.6 → main 合并提交 0b9f49a，GitHub 已同步），线上 wan.kaogong.art 仍未部署（部署是站长动作）。**

## 一、包内是什么

| 项 | 状态 |
|---|---|
| 本包版本 | **v17.8.6 / 17.8.6 / wanyu-shell-v49（正式发布态）** |
| `网站/` | 唯一正式产品（canonical 全链产物，含 .gz 预压缩，144MB） |
| `项目源码/` | canonical 数据真源 + 构建工具链 + 测试 + 文档（source_data 458MB / 受锁 data 154MB 全量在包） |
| `.git/` | 全量历史（main=0b9f49a；远端 GitHub `2421873411a-rgb/wanyu-zegang` 私有仓） |
| `发布资料/releases/v17.8.6/` | 不可变发布记录（manifest.json + SHA256SUMS 58 文件） |
| 排除项 | `网站.previous/`（回滚点）、`网站-lite/`（遗留分叉副本，删除待站长决策）、node_modules、__pycache__、*.db |

解压/恢复流程与以往一致：SHA256 校验 → 解压覆盖 → `python 项目源码/verify_sources.py` → 冒烟。

## 二、v17.7.1 → v17.8.6 之间发生了什么（三轮外部审查驱动）

1. **v17.8.5-RC1~RC3（信任收敛/构建图反转）**：canonical/cycles/*.json（wanyu-cycle-bundle/v1）成为唯一正式数据输入；legacy HTML 加载器归档至 `tools/anhui_web/archive/`；fail-closed 门禁（invariants.py）；110 条跨市重复行生命周期收口（三重等值），116 撞码成绩归属清零；测试 24 失败 → 0。
2. **v17.8.5 正式发布**（tag 5f24303 → 28d4b1d）。
3. **v17.8.6 Release Integrity Hotfix（本包重点）**：
   - **RB-01**：正式发布链退役单文件 HTML（方案 A）——单文件降级为遗留存档（`--legacy-single-file` 显式链，非发布门禁）；`release.py` 重写为共享流水线 `release_pipeline.py`：precheck(git 干净) → verify_sources → validate_canonical → audit --check → staging 构建 → 受锁输入 9 文件零漂移 → verify_site → 三烟测（WANYU_SITE_DIR=staging）→ 发布资料 manifest → **原子提升**（网站→网站.previous→staging→网站，失败自动回滚）。真实全链运行 RELEASE-EXIT=0。
   - **RB-02**：clean_rebuild 锁只读——verify_sources 最先、全链零 gen_sources_lock 引用、审计只 `--check`、受锁输入字节零漂移（不自签）。
   - 新增 `validate_canonical.py`（jsonschema 真 Schema + 业务守恒 + job_id/证据/溯源/score_state + 2026 回归锁定 8511/8401/110/12006/11883）。
   - 负向套件 `tests/test_release_integrity.py`（锁篡改/目录新文件/规范损坏 7 例/staging 失败安全/precheck 豁免）。
   - 单文件遗留测试重分类 `tests/test_single_file_legacy.py`；正式套件 27 模块 **222/0/0**。
   - 数据事实变化：**无**（110 生命周期、116 归属、三年 metrics 逐字节未动）。
   - 详见 `docs/audit/v17.8.6-final-acceptance.md`（含 5 次真实运行事件记录）。

## 三、拿到包后怎么验收（按顺序）

```powershell
# 1) 哈希校验（对照同名 .sha256.txt）
# 2) 锁校验（14 项全一致才继续）
python 项目源码/verify_sources.py
# 3) canonical 独立验证（schema+守恒+溯源+2026 回归锁定）
python 项目源码/tools/anhui_web/validate_canonical.py --root .
# 4) 空目录全链重建门禁（事实闸+verifier+audit --check）
python 项目源码/tools/anhui_web/clean_rebuild.py
# 5) 正式套件（222/0/0）
cd 项目源码; python -m unittest <见 tools/anhui_web/release.py FORMAL_TEST_MODULES>
# 6) 本地预览
cd 网站; python -m http.server 8100 --bind 127.0.0.1
```

版本唯一真源 = `项目源码/release.json`；SW/资产版本注入全自动；发布一律走 `python 项目源码/tools/anhui_web/release.py`（干净树 + Chromium 必需；`--allow-missing-browser` 会标记 degraded_validation 且禁止打 tag）。

## 四、已知问题与待办

- **F1（唯一已知功能缺陷，建议 v17.9 优先修）**：检索页「首次输入专业+回车」三级匹配降级为仅不限专业 1,249 条（0 明确含/0 类内）；任何后续重渲染即恢复正常（法学：明确含 434/类内 979/不限 1,199，与数据精确吻合）。根因指向 change 渲染与 major_index 异步重渲染的写序竞态；烟测盲区。2026-09-06 内置浏览器全站审计的其余 12 视图全部正常（报告在会话记录）。
- **站长待办**：①线上部署 v17.8.6（nginx 开 `gzip_static on`）②网站-lite 删除决策 ③109 行（110 减 1 条保留口径见审计）物理删除待官方职位表核对 ④COS 月备恢复演练 ⑤百度网盘备份本包。
- v17.9 议题：CI（windows+ubuntu）/main 保护/PR-only、fetch_sources 自动拉源、单文件重建引擎、重放等价归一、F1。

## 五、关键坐标

- GitHub：main = `0b9f49a`，tag `v17.8.6` 指向该合并提交；v17.8.5 tag（5f24303/28d4b1d）未动。
- 发布记录：`发布资料/releases/v17.8.6/manifest.json`（git_commit=98f8cdf、verified、tag_allowed=true）。
- 审计链：`docs/audit/`（RC1→RC3→v17.8.6-baseline→v17.8.6-final-acceptance）。
- 推送走 Clash 代理：`git -c http.proxy=http://127.0.0.1:7897 push`；PR 合并端点是 PUT。
