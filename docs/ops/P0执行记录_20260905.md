# P0 执行记录（2026-09-05 · 升级规划 6.3→9.5 · 保命+发布阶段）

## 已完成（本机自动执行）

1. **Git 化**：仓库根 `择岗/`，分支 `main`，基线提交 `9f05538`，标签 `v17.7.2-baseline`；423 文件入库（代码+模板+测试+文档+站点数据）。
   - 排除（COS 月备兜底）：`项目源码/source_data/` 446MB、`项目源码/tools/anhui_web/data/` 133MB、`网站-lite/` 125MB、`*.db`
   - 换行纪律：`core.autocrlf=false` + `core.eol=lf`（保证字节级测试稳定）
2. **运维脚本**：`项目源码/tools/ops/backup_wanyu.sh`（COS 日备 git bundle+站点包 / 月备大数据三件套，含 SHA256SUMS 对账与恢复演练步骤）、`check_ssl_expiry.sh`（<21 天告警）。
3. **SSL 实查**：wan.kaogong.art 证书至 2026-12-01，剩 87 天，OK。
4. **wan-api 冻结声明**：**维持暂缓启用**。启用前置 = deploy.sh 十项修复清单全过 + python-jose CVE 换库（PyJWT 或 3.4.0）+ Git/备份/监控就绪 + 回滚演练一次。在此之前任何人不得执行其 deploy.sh。

## 待站长完成（外部凭据/决策，各 ≤15 分钟）

| # | 事项 | 步骤 |
|---|---|---|
| 1 | **Gitee 私有远端** | Gitee 建私有空仓（建议名 `wanyu`）→ `git remote add origin git@gitee.com:<你>/wanyu.git`（SSH key 放 ~/.ssh，权限 600，绝不入库）→ `git push -u origin main --tags` |
| 2 | **COS 备份开通** | 腾讯云控制台建私有桶 → 本机/服务器装 coscli 并 `coscli config init` → `export COS_BUCKET=<桶名-APPID>` → 跑一次 `backup_wanyu.sh daily` → **做一次恢复演练** → 配生命周期（daily 7 天/weekly 4 版/monthly 180 天） |
| 3 | **UptimeRobot 监控** | 免费注册加 3 条监控：①https://wan.kaogong.art/maintainable/index.html（5 分钟）②站点任一数据 JSON ③首页关键词"安徽"；告警接邮箱 |
| 4 | **S1 密钥重置确认** | 腾讯云控制台确认历史 `服务器密钥_111.pem` 对应密钥对已重置/销毁（唯一高危悬置项） |
| 5 | **网站-lite 删除** | v17.7.2 起与网站 UI 层一致、数据层仍旧；建议删除（COS 月备已兜底） |
| 6 | **发布 v17.7.2** | 门禁全绿可发布；部署时 nginx 加 `gzip_static on;`；发布后跑 `check_ssl_expiry.sh` 与首页冒烟 |

## 验收口径

- `git log --oneline` ≥2 个提交、`git tag` 含 v17.7.2-baseline ✓（本机已达成）
- 远端 push 完成 + COS 恢复演练通过 + 监控告警可达 → 基建维度 5.5→7.5 达成
