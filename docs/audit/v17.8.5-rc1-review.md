# v17.8.5-RC1 外部复核基线（2026-09-05）

> 本文档固定 RC1（commit b1d114e）被外部复核后的事实基线，作为 `fix/v17.8.5-rc2` 的出发点。
> 结论：功能性收口基本完成（复核 7.8/10），工程性收口未完成——**当前成品尚不能由当前 builder 原生再生成**，必须 RC2。

## 一、固定事实（commit b1d114e）

- release = v17.8.5；service worker = wanyu-shell-v48；资产版本 17.8.5。
- 分支状态：`main` 与 `fix/v17.8.5-trust-convergence` 同指 b1d114e；main 未设分支保护（CI/保护属 v17.9.0）。
- 2026 口径：raw_posts = 8511，active_posts = 8401，excluded_posts = 110；raw_recruits = 12006，active_recruits = 11883；score_unresolved = 0，score_resolved = 116。
- 110 条跨市重复行已带 `record_status=duplicate` + 排除证据（`tools/anhui_web/d2_resolution_report_20260905.txt`），来源标记为华图快照 `source_note` 内“疑似重复收录”（构建输入自带，可作 builder 生命周期交叉校验）。

## 二、复核确认的 P0/P1 问题（RC2 全部纳入）

| 编号 | 问题 | 已核实事位 |
|---|---|---|
| P0-1 | builder 不保留 raw_total/excluded/record_status_model（`_summary_meta` 白名单缺失）；`_LITE_ROW_KEYS` 无 record_status → 重建会丢字段；`_major_city_payload`/`_city_trend`/catalog/positions/changes/palette 全部吃 raw rows | build_maintainable_site.py |
| P0-2 | `audit.audit.score_lists.unresolved=116` 残留；`audit.audit.gaps[1]/known_gaps[1]/unverified_scope[1]` 仍写“成绩附件存在 116 个无法唯一匹配项”；verifier 未检查 `audit.audit` 层 | three_year_audit.json 源头 + audit_three_years.py 生成器 |
| P0-3 | site-manifest 2026：posts=8511、recruits=12006（raw 语义占用用户字段名），缺 raw_recruits | build_maintainable_site.py manifest 装配 |
| P1-1 | release.py：playwright-core 有库无 Chromium 时仅警告跳过；`--allow-missing-browser` 只在报错文案里存在，argparse 无此参数；package.json 无标准 scripts | release.py / package.json |
| P1-2 | sources.lock 目录项 sha256=null 且 fetch_sources.py 不校验目录内容（目录存在即“一致”）；无 fetch 能力却叫 fetch_sources | fetch_sources.py / gen_sources_lock.py |
| F/K | clean rebuild 全链被 `deliverables/legacy_v11/皖域择岗总览.html` 缺失阻断（2026 候选含 `安徽公考数据网页/`、`deliverables/`，均不存在） | unified_cycle_bundle._candidate_files |

## 三、RC1 期间已核实的恢复努力

- 百度网盘全库无 `*总览*` 文件；`/AI云空间/皖域择岗/项目源码/deliverables` 与本机一致（仅 releases/MANIFEST/HANDOFF/CHANGELOG/制作说明）。
- 本机工作区（D:\AI\zcode\皖域择岗）无任何 legacy HTML、无历史压缩包；E 盘不存在。
- **结论：legacy_v11 恢复需要站长侧动作（COS 月备回捞 / 另一设备 / 历史交付介质），RC2 内保持 BLOCKED_BY_EVIDENCE。**

## 四、RC2 应对策略（本分支）

1. record_status 生命周期集中化（Python 单一实现 + overrides 构建输入），builder 全模块 active 口径派生，jobs_lite 只含 active。
2. 116 审计语义迁移到 resolved history（源头文件 + 生成器同步修），verifier 全投影检查。
3. manifest 增加 raw_recruits/recruits=active，posts=active_posts(deprecated)。
4. browser smoke 真 fail-closed（Chromium 缺失=FAIL，`--allow-missing-browser` 真实现且降级标注 degraded_validation）。
5. sources.lock 目录级 rollup SHA256 + 更名 verify_sources.py。
6. 在 legacy_v11 恢复前：以「生产 jobs.json 原始行为行源的派生层金测」证明 builder 派生逻辑能原生复现 8401/11883/110/0 全部事实；全链从零重建如实保持 BLOCKED_BY_EVIDENCE，不打 v17.8.5 正式 tag。
