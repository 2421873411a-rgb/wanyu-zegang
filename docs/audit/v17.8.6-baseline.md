# v17.8.6 Release Integrity Hotfix 基线（2026-09-05）

> 基线：main = 5022502（PR #1 merge）。分支：`fix/v17.8.6-release-integrity`。
> 外部复核：9.0/10。RC3 主体确认达成（canonical 数据链/口径/生命周期/116/维护站 clean rebuild/fail-closed/rollup/PR 流程），本轮不重议。

## 一、固定事实（不变）

release=v17.8.5 / asset=17.8.5 / SW=wanyu-shell-v48；
2026：raw_posts=8511 / active_posts=8401 / excluded_posts=110 / raw_recruits=12006 / recruits=11883 / score_unresolved=0 / resolved_score_count=116。

## 二、两个 Release Blocker（本轮唯一靶子）

| # | 问题 | 根因 |
|---|---|---|
| RB-01 | `release.py → build_and_sync()` 仍执行 build_pages → build_single_file_html → canonical 无 page_content → RuntimeError（23ed5de 的守卫正确，但正式发布入口因此不可执行） | 正式 release 链仍背着旧单文件构建流程 |
| RB-02 | clean_rebuild 顺序 = 审计重生成 → gen_sources_lock → verify——先改锁再验锁，被污染的数据会被"重新盖章" | lock 在发布链中当了输出；正确语义=发布期间 lock 只能是输入 |

## 三、架构决定（§5–§6）

**方案 A（推荐采纳）**：维护站 `网站/` = 唯一正式产品（canonical → maintainable）；
`皖域择岗总览.html` = legacy archival artifact，不再阻断 release（`--legacy-single-file` 显式走旧链，默认关闭）。单文件重建引擎保留 v17.9 计划。

## 四、本轮交付面

1. release_pipeline.py 共享流水线（preflight→verify→canonical→tests→staging→verify→smoke→manifest→promote）；clean_rebuild = pipeline+tempfile+不 promote；release = pipeline+staging+promote。
2. audit_three_years `--check`（重算内存比对 committed，不一致 FAIL，不覆盖）；clean/release 不再改 locked inputs（verify_inputs_unchanged 前后哈希一致）。
3. validate_canonical.py：真 JSON Schema（jsonschema）+ 业务守恒（raw_recruits/recruits 逐行求和）+ 2026 回归值 + 证据/ID/provenance/score_state。
4. `_score_resolution_total` fail-open 修复。
5. 负向测试：锁污染不自签 / source_data 新文件 rollup / canonical 各类损坏 / staging 失败不污染生产。
6. 测试重分类：单文件 legacy 测试移入 `tests/test_single_file_legacy.py`（正式套件 0 functional skip）。
7. 版本：release.json → v17.8.6 / 17.8.6 / wanyu-shell-v49（唯一手改点，其余自动贯穿）。

## 五、tag 纪律（§36）

feature branch → PR → checks → merge main → 确认 main SHA → **tag 指向 main merge commit**（禁止先 tag 分支再 merge）。
