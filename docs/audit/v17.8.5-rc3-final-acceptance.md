# v17.8.5-RC3 正式验收（finalization）· 2026-09-05

> 分支：`fix/v17.8.5-rc3-finalization`（自 7ef107f）。任务书：v17.8.5-RC3 → v18.1 执行总计划。
> 结论：**六个零**全部达成 → 具备正式 `v17.8.5` tag 条件（本文档即为 §22 验收证据）。

## 〇、验收标准逐字回应（§三）

> 在不依赖历史 HTML 成品、不依赖人工修改网站 JSON、不依赖 Python assert 的情况下，从受控数据真源执行正式 release 流程，得到相同的 8401/11883/110/0/116 事实，并实现所有测试 0 failure。

- **不依赖历史 HTML 成品**：`build_unified_bundles` 唯一正式入口=canonical（HTML 反向读取整体移至 `archive/legacy_html_loader.py`，正式链 import 禁止由测试锁定）；正式链所有行源 builder（审计/索引/job_history/major_index）已重指 canonical。
- **不依赖人工修改网站 JSON**：生产树 = `assemble_maintainable_site(canonical)` 全链产物（b9b112a）；生命周期输入来自 overrides v2 证据链（三重等式），不读任何产物。
- **不依赖 Python assert**：生产门禁全部显式异常（`invariants.BuildInvariantError`/`RecordLifecycleError`）；`python -O` 下门禁仍生效。
- **受控数据真源 → 相同事实**：`clean_rebuild.py` 空目录全链 PASS（8511=8401+110/12006/11883/8401-lite/0/116）。
- **0 failure**：Python 211 例 0 fail（2 个有据 skip）；Node 14+1 例 0 fail；浏览器 3 烟测 0 fail；semantic verifier 302/0；sources 14 项；clean rebuild PASS。

## 一、阶段清单（§34 模板要点汇总）

| 阶段 | 状态 | 关键产物 | commit |
|---|---|---|---|
| A 基线冻结 | PASS | docs/audit/v17.8.5-rc3-baseline.md；分支纪律（main 不直推） | b891d6e |
| B fail-closed | PASS | invariants.py；builder/verifier/migration 的 assert 与 `or 0` 全部清除 | da3fab7 |
| C 严格枚举 | PASS | ALLOWED_STATUSES；未知状态抛 RecordLifecycleError；verifier C1 枚举扫描 + C2 排除证据三字段 | 177706a |
| D 0 测试失败 | PASS | rc3-test-failure-map.md（24→0，含 2 有据 skip 与重放分叉登记） | 472e9af（及测试批） |
| E/F canonical | PASS | canonical/cycles/*.json（wanyu-cycle-bundle/v1）+ schema.json + 种子迁移证明 | b8cbb93 |
| G/H 构建图反转 | PASS | unified_cycle_bundle→canonical；legacy loader 归档隔离；审计/索引重指 | b8cbb93/23ed5de |
| I HTML=OUTPUT | PASS（诚实降级） | 正式链零 HTML 输入（O 测试锁定）；单文件 v12 重建引擎列 v17.9（快照丢失有据 skip） | 23ed5de |
| J 证据链去循环 | PASS | overrides v2：canonical+孪生+line 归属不对称独立重推 110；三重等式（标记=重推=提交）+ 116/0 交叉核对 | f24718b |
| K 双真源退役 | PASS | phantom_codes_2026.json 出锁；生命周期唯一输入=overrides | f24718b |
| L runtime 统一 | PASS | 三周期 cycleRuntime row_count/raw_row_count/score_unresolved 同构（2024: 10017/10017） | b9b112a |
| M 地市维度 | PASS（schema 层） | canonical city_dimension（16市+省直；off_dimension 三周期=0；source_city 留位待回填） | b8cbb93 |
| N clean rebuild | PASS | clean_rebuild.py：审计重生成→重锁→空目录 assemble→事实闸→verifier | f31fffb |
| O anti-circular | PASS | test_build_input_purity.py（部署树/生成物禁入正式输入；O:OUTPUT-SIDE 显式豁免） | f31fffb |
| P release 硬门槛 | PASS | --skip-tests 需 WANYU_DEV=1；degraded_validation → tag_allowed=false | 472e9af |
| Q 文档真源 | PASS | generate_status_doc.py + docs/current-status.md + 门禁测试；README 重写 | 472e9af |
| R 终验+发布 | PASS | 六个零 + tag v17.8.5 + PR 合并 | 本提交 |

## 二、最终事实（2026，§21/§22 对照）

raw_posts=8511 / active_posts=8401 / excluded_posts=110 / raw_recruits=12006 / recruits=11883 / unresolved=0 / resolved=116；
jobs raw=8511；jobs_lite/positions/major_city/catalog=8401；major_index stats.rows=8401；其他模块零 excluded job_id（verifier 锁定）。

## 三、门禁全景（终态）

- Python：211 例，0 failure / 0 error / 2 documented skip。
- Node：14 pass / 0 fail + datastore PASS。
- 浏览器：maintainable / major_city / ui_upgrade 三烟测 PASS（Chromium 已标准化安装）。
- Semantic verifier：302/0（含 C1 枚举、C2 证据、resolved 配对、stale 语义扫描、输入纯度绑定）。
- Sources：14 项（12 文件 + 2 目录 rollup）全一致；负向测试有效。
- Clean rebuild：PASS（空目录、零产物输入）。
- check_release.sh：全绿。

## 四、残余风险（诚实登记）

1. 重放与 canonical 的登记分叉（rc3-test-failure-map.md §三）：v17.9 结构化重放引擎任务。
2. 单文件 v12 重建引擎：v17.9；冻结快照恢复介质=COS 月备/另一设备（站长）。
3. 110 行物理删除：待官方职位表核对（站长）。
4. `source_city` 回填、main 分支保护/CI（v17.9 S 阶段）。
