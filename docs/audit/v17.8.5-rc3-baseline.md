# v17.8.5-RC3 基线（2026-09-05）

> 基线 SHA：7ef107f（RC2 合并后的 main）。分支：`fix/v17.8.5-rc3-finalization`。
> 外部复核：8.6/10。RC2 六项确认落地；RC3 目标=修构建图（HTML 退出输入图）、修门禁（fail-open 清零）、修测试（0 failure）、修证据真源（去循环/去双真源），然后正式打 v17.8.5。

## 一、固定事实（不变，本轮不动业务数字）

2026：raw_posts=8511 / active_posts=8401 / excluded_posts=110 / raw_recruits=12006 / recruits=11883 / score_unresolved=0 / resolved=116。
jobs raw=8511；jobs_lite/positions/major_city/catalog=8401；其他模块不得含 excluded job_id。

## 二、RC3 输入问题清单（本轮全部处理）

| # | 问题 | 阶段 |
|---|---|---|
| 1 | unified_cycle_bundle 仍从 legacy HTML 反向读取（2026 候选：legacy_v11、安徽公考数据网页、deliverables；缺失即抛错） | E/F/G/H |
| 2 | 生产门禁用 Python assert（`python -O` 关闭）；`_unresolved_score_count`/verifier `_unresolved` 非法值静默归 0（fail-open） | B |
| 3 | record_status 未知值静默排除（拼写错误不报错） | C |
| 4 | 测试 24 failure（含 test_v17_salary_and_motion 硬编码 BUILD_VERSION="v17.6.4" 与旧设备 `site/` 路径）；release.py check=True 下无法走通 | D |
| 5 | 生命周期证据循环：gen_record_status_overrides 读网站成品 jobs.json 生成 overrides | J |
| 6 | phantom_codes_2026.json 仍在 sources.lock 必填清单（双真源） | K |
| 7 | 三周期 cycleRuntime 行数结构不统一（2024/2025 缺 raw_row_count） | L |
| 8 | 地市维度（source_city/city/subregion）未成正式 schema | M |
| 9 | main 无保护、无 CI、无 PR 流程（CI/保护=v17.9，本轮先 PR 合并纪律） | A/R |
| 10 | README_先看这里.md 过期（v17.6.4/116 待归属/8511 旧口径） | Q |

## 三、结构性事实（本轮勘察结论）

- `tools/anhui_web/data/cycles/{2024,2025}/` 存在完整周期源：`score_lists.json`（10/15MB，keyed.unresolved=[]）、`cycle.json`（label/status/stats）、官方文件族（ahsk/hire/official/daxian/guokao/huatu）。
- `all_majors_2024.json`（4243 行拼音字段源表）是**归一化前中间源**，不是审计 payload——三周期 canonical 种子统一走任务书 G 钦定的 `audited_production_snapshot`（附迁移证明文档）。
- `build_pages.py` 是数据→HTML 的生成器（MASTER 引用只是输出侧交叉链接）；单文件 v12 的组装（`single_file_site.build_single_file_html`）依赖 `bundle.page_content`（legacy 页面内容）——**正式链改为 canonical→维护站全链；单文件保持冻结快照，其重建引擎列入 v17.9**（HTML 永远不再作为任何正式输入）。
- 2026 成品行含跨会话累积修正（哨兵 629 复活、city_norm、D2 打标），不可由 source_data 零成本重放——故 canonical 种子以 7ef107f 的审计快照冻结，迁移文档记录可验证/不可验证边界。

## 四、当前 24 个失败清单（基线）

见 `/tmp/rc2_current_fails.txt` 快照分类：
- legacy 依赖类（build_pages/unified HTML/three_year_audit 页面读取/deliverables 缺失）：17 ERROR——canonical 改造后随 build_unified_bundles 复活而消解。
- test_v17_salary_and_motion 3 FAIL：BUILD_VERSION 硬编码 + `site/` 设备路径 + salary nav 断言过期。
- test_maintainable_site disk-verifier 指向 deliverables/maintainable（本机无）。
- 其余为上述类的派生呈现。
